<!--
id: "giraffe-meet-cotton"
created: "Wed Dec 31 13:54:47 2025"
attribution: DTK
use: "Chapter 5 exercises"
status: "ready-to-use"
chapter: 5
mode: "exercise"
-->

{{< include ../exercise-header.qmd >}}

::: {#exr-giraffe-meet-cotton}
We have defined for your convenience several one-input functions named `f1()` through `f5()`. Graph each of them, one at a time, on the domain -5 to 5 and, from the graph, read off the requested information. (Hint: All you need is the *name* of the function, you don't have to worry about how we constructed it.)

```{webr}
#| echo: false
#| autorun: true
f1 <- mosaicCalc::doodle_fun( ~ z, seed = 504)
f2 <- mosaicCalc::doodle_fun( ~ z, seed = 506)
f3 <- mosaicCalc::doodle_fun( ~ z, seed = 508)
f4 <- mosaicCalc::doodle_fun( ~ z, seed = 510)
f5 <- mosaicCalc::doodle_fun( ~ z, seed = 512)
```

```{webr}
#| caption: Make your plots here.
slice_plot(f1(z) ~ z, domain(z = -5:5))
```

1. For `f1()`, for what input does the output reach its maximum value? (Select the closest value.)

```{mcq}
#| label: gmc-1-lesle
#| inline: true
1. -4
2. -3
3. -2 [correct]
4.  0
5.  2
6.  3
7.  4
```

2. For `f2()`, what is the input where the output crosses zero?  [Pick the closest answer.]

```{mcq}
#| label: gmc-2-loie
#| inline: true
1. -3
2. -1 [correct]
3. 0 
4.  1
5.  3
```

3. For `f3()`, what is the minimum value of the output? [Pick the closest answer.]

```{mcq}
#| label: gmc-3-yee
#| inline: true
1. 0
2. -10
3. -20
4. -30 [correct]
```

4. For `f4()`, at what input does the output cross zero? [Pick the closest answer.]

```{mcq}
#| label: gmc-4-rwsle
#| inline: true
1. -4
2. -3
3. -2 
4.  0
5.  2
6.  3 [correct]
7.  4
```

5. For `f5()`, at how many different inputs does the output cross zero? 

```{mcq}
#| label: gmc-5-qdr
#| inline: true
1. 0
2. 1
3. 2 
4. 3 [correct]
5. 4
```
:::
 <!-- end of exr-giraffe-meet-cotton -->
